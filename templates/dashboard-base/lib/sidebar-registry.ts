export type SidebarItem = {
  slug: string;
  label: string;
  week: number;
};

// 주차 스킬이 이 두 마커 사이에 항목을 누적 삽입합니다. 직접 편집하지 마세요.
export const sidebarItems: SidebarItem[] = [
  // <SIDEBAR_REGISTRY>
  // </SIDEBAR_REGISTRY>
];
